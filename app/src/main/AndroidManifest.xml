<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.com.fyweddingplanner">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!--
 The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but are recommended.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="com.app.fyweddingplanner.WeddingApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyMaterialTheme">
        <activity
            android:name="com.app.fyweddingplanner.splachScreen"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.app.fyweddingplanner.auth.LoginActivity"
            android:label="@string/app_name" />
        <activity
            android:name="com.app.fyweddingplanner.auth.SignUp"
            android:label="@string/title_activity_sign_up" />
        <activity android:name="com.app.fyweddingplanner.activity.MainActivity" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.app.fyweddingplanner.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name="com.app.fyweddingplanner.OfferDetails"
            android:label="@string/title_activity_offer_details"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name="com.app.fyweddingplanner.WeddingPlannerDetails"
            android:label="@string/title_activity_wedding_planner_details"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name="com.app.fyweddingplanner.rating"
            android:label="@string/title_activity_rating"></activity>
        <activity android:name="com.app.fyweddingplanner.FragmentGallerySlider" />
        <activity
            android:name="com.app.fyweddingplanner.CarRentDetails"
            android:label="@string/title_activity_car_rent_details"
            android:theme="@style/MyMaterialTheme"></activity>
    </application>

</manifest>